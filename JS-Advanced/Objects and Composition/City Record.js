function cityRecord(city,population,treasury) {

    const object = {
        name: city,
        population: population,
        treasury: treasury
    };

    return object;
}
//cityRecord('Tortuga',7000,15000);
